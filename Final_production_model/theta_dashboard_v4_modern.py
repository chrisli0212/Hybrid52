# NOTE: Full file with targeted changes to _RATIO_COLORS, _RATIO_WIDTHS, _RATIO_OPACITIES, and legend position in _multi_symbol_ts_chart.
# This is a targeted patch — only the constants block and legend config change.
# The rest of the file is unchanged.

# To apply: replace the three constants and the legend dict in _multi_symbol_ts_chart.
